# Netlify 배포 설정 — git push 시 자동 빌드
[build]
  command = "node scripts/build.mjs"   # data/announcements.json → dist/ 정적 생성 (의존성 0)
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# 정적 파일 캐시 (선택)
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
